{
  "name": "ghmc-election-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/db"
  ],
  "scripts": {
    "dev":   "npm run dev --workspace=packages/api",
    "test":  "npm run test --workspace=packages/api",
    "build": "npm run build --workspace=packages/api"
  }
}
